#include <complex>
#include <vector>

using namespace std;

vector<complex<double>> DFT(vector<double> a){
    int n = a.size();
    // vector<complex<double> y = vector<complex>(n,complex(0,0));
}
vector<complex<double>> FFT(vector<double> a){}